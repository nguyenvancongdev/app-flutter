
import 'package:flutter/material.dart';

class HomeEmage extends StatelessWidget {
  const HomeEmage({super.key});
  @override
  Widget build(BuildContext context) {
    return  Text(
                'dd',
                style: TextStyle(
                    fontWeight: FontWeight.w600,
                    fontSize: 15,
                    color: Colors.red[700]),
              );
  }
}